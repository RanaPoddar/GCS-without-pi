# Ground Control Station Configuration

# Server port
PORT=3000
NODE_ENV=development

# Drone 1 Pixhawk Connection
# Serial port for Drone 1's telemetry connection
DRONE1_PORT=/dev/ttyUSB0
# Baud rate for Drone 1 (typically 57600 or 115200)
DRONE1_BAUD=57600

# Drone 2 Pixhawk Connection
# Serial port for Drone 2's telemetry connection
DRONE2_PORT=/dev/ttyUSB1
# Baud rate for Drone 2 (typically 57600 or 115200)
DRONE2_BAUD=57600

# Common serial ports on Linux:
# - /dev/ttyUSB0, /dev/ttyUSB1 - USB serial adapters
# - /dev/ttyACM0, /dev/ttyACM1 - USB ACM devices
# - /dev/ttyS0, /dev/ttyS1 - Hardware serial ports
#
# To find connected devices, run:
#   ls /dev/tty* | grep -E "(USB|ACM)"
#
# To check device permissions:
#   ls -l /dev/ttyUSB0
#
# To add yourself to dialout group (required for serial access):
#   sudo usermod -a -G dialout $USER
#   (then log out and back in)
